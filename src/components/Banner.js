import React from 'react';

const Banner = () => (
  <div className="bg-gradient-to-r from-purple-400 to-pink-600 p-10 text-white text-center">
    <h2 className="text-4xl font-semibold mb-4">Discover Your Signature Scent</h2>
    <p className="text-lg">Explore our exclusive collection and find the fragrance that defines you.</p>
    <button className="mt-6 px-6 py-2 bg-white text-gray-800 font-semibold rounded-full hover:bg-gray-200">
      Shop Now
    </button>
  </div>
);

export default Banner;
